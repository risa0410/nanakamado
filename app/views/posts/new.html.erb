<main class="bg-white shadow-sm">
  <p id="notice"></p>
  <div class='container'>
    <div class='row'>

        <div class='col-md-3 text-center'>
          <h2>User info</h2>
          <table class="row justify-content-center">
            <tr>
              <% if @user.profile_image.attached? %>
                <%= image_tag @user.profile_image.variant(resize:'100x80') %>
              <% else %>
                <%= image_tag('no_image.jpg', size:'100x80') %>
              <% end %>
            </tr>
            <tr>
        	    <th>name</th>
        	  </tr>
        	  <tr>
        	    <th><%= @user.name %></th>
            </tr>
            <tr>
        	    <th>introduction</th>
        	  </tr>
        	  <tr>
        	    <th><%= @user.introduction %></th>
        	  </tr>
        	  <tr>
        	    <th>
        	      <% if current_user.following?(@user) %>
                  <%= link_to "フォローを外す", user_relationships_path(@user.id), method: :delete %>
                <% else %>
                  <%= link_to "フォローする", user_relationships_path(@user.id), method: :post %>
                <% end %>
        	    </th>
        	  </tr>
          </table>
        </div>

        <div class='col-md-9'>
        <h2>New Post</h2>
          <%= form_with model: @post, local:true do |f| %>
            <div class="form-group">
              <label for="book_title">Image</label></br>
              <%= f.file_field :post_image %>
            </div>
            <div class="form-group">
              <label for="book_title">Title</label></br>
              <%= f.text_field :title, class:"form-control book_title shadow-sm" %>
            </div>
            <div class="form-group">
              <label for="book_opinion">Body</label></br>
              <%= f.text_area :body, class:"form-control book_body shadow-sm" %>
            </div>
            <div class="form-group">
              <%= f.submit 'Create Post', class:"btn btn-success" %>
            </div>
          <% end %>
        </div>

    </div>
  </div>
</main>